# Dockerfile สำหรับ Windows
# Note: Docker on Windows ไม่สามารถควบคุม GUI ได้โดยตรง
# ควรรันโค้ดนี้บน Windows host โดยตรงแทน

FROM python:3.9-windowsservercore

WORKDIR /app

# คัดลอก requirements
COPY requirements-windows.txt .

# ติดตั้ง dependencies
RUN pip install --no-cache-dir -r requirements-windows.txt

# คัดลอกไฟล์โค้ด
COPY main.py .
COPY image.png .

# หมายเหตุ: Docker container ไม่สามารถควบคุม mouse/keyboard ของ host ได้
# แนะนำให้รันโดยตรงบน Windows แทน
CMD ["python", "main.py"]
